<h1>List of all Playoff Series in 2021</h1>

<p>There are <%= @series.count %> playoff series loaded so far.</p>

<table>
  <thead>
    <tr>
      <th>Higher Seed</th>
      <th>Lower Seed</th>
      <th>Score</th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @series.each do |series| %>
    <tr>
      <td><%= series.team1.short_name %></td>
      <td><%= series.team2.short_name %></td>
      <td><%= series.result %></td>
      <td><%= link_to "Details", series_path(id: series.id) %></td>
      <td><%= link_to "Make your Pick!", new_prediction_path(id: series.id) %></td>
    </tr>
    <% end %>
  </tbody>
</table>