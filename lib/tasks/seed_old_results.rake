task :seed_old_results do
  sherwin = User.find(1)
  joey = User.find(2)
  alex = User.find(3)
  billy = User.find(4)
  james = User.find(5)
  ellery = User.find(6)
  omar = User.find(7)
  nathaniel = User.find(8)
  steven = User.find(9)
  vinicius = User.find(10)
  jonathan = User.find(11)
  yuji = User.find(12)
end